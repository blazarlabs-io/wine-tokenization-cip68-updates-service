module Parameters where

validatorFile :: FilePath
validatorFile = "wine_validator.plutus"

blueprintFile :: FilePath
blueprintFile = "wine_contract_blueprint.json"

mintingPolicyFile :: FilePath
mintingPolicyFile = "wine_minting_policy.plutus"

atlasCoreConfig :: FilePath
atlasCoreConfig = "config_atlas.json"

adminConfig :: FilePath
adminConfig = "config_admin.json"